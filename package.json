{
  "name": "visual-chat",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Companion browser canvas for terminal LLM agents (placeholder name - never publish)",
  "scripts": {
    "dev": "vite --config src/canvas/vite.config.ts",
    "build": "tsc -p tsconfig.json && tsc -p src/canvas/tsconfig.json && vite build --config src/canvas/vite.config.ts",
    "test": "bun test",
    "start": "node dist/server/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.0",
    "vite": "^7.0.0"
  }
}
