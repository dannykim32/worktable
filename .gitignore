node_modules/
dist/
.visual-chat/
.gstack/

# agent worktrees — never track
.claude/worktrees/

# local MCP registration (machine-specific abs path; recreate per docs/setup.md)
.mcp.json

# release tarballs (built artifacts)
dist-release/
