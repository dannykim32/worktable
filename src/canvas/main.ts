// Canvas stub (issue 01). Real gallery shell lands in issue 04.
const app = document.getElementById("app");
if (app) {
  const p = document.createElement("p");
  p.textContent = "canvas up";
  app.appendChild(p);
}
